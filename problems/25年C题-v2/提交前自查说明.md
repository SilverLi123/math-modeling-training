# 提交前自查说明（可删除，不影响提交）

本文件夹已整理为可直接提交的状态。提交前请确认以下事项：

1. **论文文件**
   - `论文.pdf` 为排版终稿（29 页，正文约 24 页 + 附录支撑材料说明）。
   - `论文.docx` 为 Word 同源版（若组委会只收 PDF，可不上传 docx）。

2. **命名**：若组委会要求“队伍编号_论文.pdf”或特定命名，
   请在提交前重命名 `论文.pdf`，不要改动内部内容。

3. **支撑材料**：`支撑材料/` 目录包含完整源码、数据、结果与复现清单，
   见其中 `README.md`（有目录结构、复现命令、论文→结果对照速查）。

4. **建议自查项（提交前快速执行）**
   - 用 PDF 阅读器打开 `论文.pdf`，检查是否有缺图、乱码、页码异常；
   - 抽查支撑材料 `outputs/q2_decision/t90_banding.json` 中的三档切点
     （32.75/35.25）与论文表 6 一致；
   - 如组委会允许，可在本地 python 环境安装依赖后运行
     `python src/q2_km_comparison.py` 等轻量脚本验证输出可复现
     （固定种子 20250904，详见支撑材料 README）。

5. **压缩**：根目录 `提交_25年C题.zip` 即为可上传的压缩包；
   若组委会上传系统要求把“支撑材料”单独打包，可只传
   `支撑材料/` 目录或其中 `README.md` 指定的内容。

6. **请勿提交**：本说明文件、`docs/` 中的内部记录（问题分析/建模日志/结果）
   若组委会严格限定支撑材料仅含“代码+结果+图表”，可在上传前删除
   `docs/` 与 `results/` 中的内部 md 记录（保留 json 复现清单）。
