# 提交前自查说明（v2·融合终稿）（可删除，不影响提交）

本文件夹已整理为可直接提交的状态。提交前请确认以下事项：

1. **论文文件**
   - `论文.pdf` 为排版终稿（22 页，融合终稿扩写版；SHA-256 `35C1611B…`，
     与 `工程归档/paper/latex_final/论文.pdf` 同源同 hash；确定性编译
     SOURCE_DATE_EPOCH=1752307200，可逐字节复现）。
   - `论文.docx` 为 Word 版（由同一 LaTeX 源经 pandoc 转换，公式 OMML；
     若组委会只收 PDF，可不上传 docx）。

2. **命名**：若组委会要求“队伍编号_论文.pdf”或特定命名，请在提交前重命名
   `论文.pdf`，不要改动内部内容。

3. **支撑材料**：`支撑材料/` 目录包含完整源码、输出、复现清单与论文用图，
   结构、复现命令与“论文→结果对照”见根目录 `README.md`。

4. **建议自查项（提交前快速执行）**
   - 用 PDF 阅读器打开 `论文.pdf`，检查缺图、乱码、页码异常（22 页）；
   - 抽查支撑材料 `outputs/q4_model/experiment.json`：AUC 0.808、
     PR-AUC 0.460、Brier 0.160、嵌套阈值中位 0.491 → 敏感度 68.7%/特异度 77.3%，
     与论文表一致；
   - 抽查 `outputs/q23_measurement_sensitivity/scale.json`：σ̂_m≈0.1237、
     区间 [0.0817, 0.1677]，与论文一致；
   - 抽查 `outputs/metrics/q1_lmm_metrics.json`：ICC≈0.7283、主体留出
     RMSE≈0.514/R²≈−0.030，与论文一致；
   - 如组委会允许，可在本地 python 环境安装依赖后运行 `src/q1_lmm_baseline.py`
     等轻量脚本验证输出可复现（固定种子 20250904，详见 README）。

5. **压缩上传**：若上传系统要求打包，请把 `论文.pdf`（可加 `论文.docx`）与
   `支撑材料/` 一起压缩（或按组委会提示只传论文与支撑材料 zip）；本目录未预生成
   压缩包，上传前自行压缩即可。

6. **请勿上传**：本说明文件、`工程归档/`（LaTeX 源、data、全量 outputs 等仅供
   追溯/复跑）；若组委会严格限定支撑材料仅含“代码+结果+图表”，可在上传前从
   `支撑材料/docs/` 移除内部 md 记录（保留 json/csv 复现产物）。
