# 2025 CUMCM C 题：NIPT 的时点选择与胎儿异常判定——题目分析与模型建议

> 阶段：模型合同修订（2026-09-07）。本地已实现 Q1 LMM baseline；GAMM 与 Q2–Q4 尚待实验验证。本报告不包含最终赛题答案。2025 年竞赛论文格式与提交规则尚未在本阶段核验。

## 1. 题目与附件快照

| 文件 | 用途 | SHA-256 |
|---|---|---|
| `C题.pdf` | 2025 年高教社杯全国大学生数学建模竞赛 C 题题面 | `D54400F4BB9920C0D37F15F5B6BC161AA281C4B32C03C063EFDEB739716EAC08` |
| `附件.xlsx` | 男胎、女胎 NIPT 检测数据 | `14827156218BD4F7E4F16DB4AA6D9F757C6648379E038AE6C6B58383648614AF` |

附件保持只读。读取时显式把第一行作为表头，未把第一条数据误判为列名。

## 2. 关键数据事实

| 数据集 | 检测记录 | 独立孕妇 | 每人记录数中位数（范围） | 孕周范围 | BMI 范围 |
|---|---:|---:|---:|---:|---:|
| 男胎 | 1082 | 267 | 4（1–8） | 11.00–29.00 周 | 20.70–46.88 |
| 女胎 | 605 | 147 | 4（1–9） | 11.00–28.43 周 | 25.59–45.72 |

必须据此处理以下结构：

1. **独立样本单位是孕妇，不是检测行。**男胎和女胎均有大量重复测量，训练/验证划分必须按孕妇代码分组。
2. 男胎 937/1082 条记录达到 `Y 染色体浓度 ≥ 4%`；267 名孕妇中，217 人首次观测即达标（左删失），43 人在观测区间内跨过阈值，7 人始终未达标（右删失）。因此不能把“第一次观测到达标”当作精确真实达标时间。
3. 男胎有 34 名孕妇在某次达标后又观测到低于 4%，表明观测值并非严格单调，检测误差或生物波动不能忽略。
4. 男胎有 18 组同一孕妇、同次抽血、同日期和同孕周的重复检测；其 `Y` 浓度绝对差中位数约为 0.00452、均值约为 0.00546，可用于估计测量误差，但样本组数较少，必须同时给出 Bootstrap 不确定性。
5. 女胎异常标签 `AB` 非空 67/605 条，涉及 44/147 名孕妇；阳性类别不平衡。女胎 605 条记录的 `胎儿是否健康` 均为“是”，该列不能作为异常分类标签，也不能证明 `AB` 判定的临床真伪。
6. 女胎 `AB` 标签在 43 名孕妇的不同检测记录间发生变化，说明问题 4 是检测记录级判定，但交叉验证仍要按孕妇分组，防止同一人的信息泄漏。
7. BMI 由身高和体重确定。问题 3 若同时直接加入身高、体重和 BMI，会产生确定性/强共线性；建议保留 BMI 与身高，或在 BMI、身高、体重三者中只取两个。
8. 测序读段数、比对率、重复率、GC 含量和过滤率在检测完成后才可获得。它们可用于问题 1 和 4 的检测质量解释，但不能作为“检测前选择时点”的可用预测变量。
9. 男胎“末次月经”有 12 个缺失值，且检测日期列存在 Excel 日期对象与 `YYYYMMDD` 整数混合类型。模型直接采用题面给出的“检测孕周”，不使用末次月经反推孕周；日期字段只用于同次检测识别和一致性检查。

## 3. 四个子问题的结论目标

| 子问题 | 最终应给出的结论 | 主要评价口径 |
|---|---|---|
| 问题 1 | Y 浓度与孕周、BMI 及必要质控变量的方向、形状、效应量和显著性 | 受试者内相关被控制；效应区间、似然检验、残差与外推边界 |
| 问题 2 | 仅依据检测前 BMI 的连续分组区间及每组推荐 NIPT 时点 | 达标/可检概率、发现延迟风险、组内稳定性、误差敏感性 |
| 问题 3 | 综合检测前多因素后，仍以 BMI 表达的分组和时点策略 | 受试者级外推误差、时间校准、达标概率、决策风险 |
| 问题 4 | 女胎 T13/T18/T21 异常及总体异常的可解释分类规则 | PR-AUC、灵敏度、特异度、MCC、校准与受试者级 Bootstrap |

## 4. 总体推荐路线

采用“解释—预测—决策”三层结构：

| 层次 | 核心问题 | 输出 |
|---|---|---|
| 解释层 | Y 浓度如何随孕周、BMI 和测序质量变化 | 平滑效应、交互曲面、置信区间与显著性 |
| 预测层 | 某孕周观测到 `Y≥4%` 的概率 | 边际达标概率、校准与观测支持 |
| 决策层 | 如何平衡过早检测不可靠与过晚发现风险 | BMI 连续分组、每组推荐时点、风险敏感性 |

```text
只读数据审查
  → 问题1：LMM baseline + GAMM 纵向浓度模型
  → 问题2：BMI-only 达标时间/达标概率模型 + 精确动态规划分组
  → 问题3：多因素概率模型 + 同一决策优化器
  → 问题4：Z-score 规则 baseline + 成本敏感、可校准分类器
  → 全程按孕妇分组验证 + 测量误差蒙特卡洛/Bootstrap
```

这条路线让问题 1–3 共享一套统计语义：先估计个体在候选孕周的 Y 浓度或达标概率，再把概率送入分组和时点决策；避免四问各套一个互不相干的算法。

## 5. 模型合同与具体建议

### 5.1 问题 1：纵向关系与显著性

#### 推荐模型

**Baseline：线性混合效应模型（LMM）。**令 `y_ij` 为孕妇 `i` 第 `j` 次检测的 Y 浓度，先作 logit 变换；最小 baseline 的固定效应只使用孕周、BMI 和孕周×BMI，并加入孕妇随机截距：

$$
\operatorname{logit}(y_{ij})=\beta_0+\beta_1t_{ij}^{c}+\beta_2B_{ij}^{c}
+\beta_3t_{ij}^{c}B_{ij}^{c}+b_{0i}+\varepsilon_{ij}.
$$

该公式用于先检验最基本的线性解释力，禁止在 baseline 中通过加入大量质控变量抬高性能。聚类稳健 OLS 只作读取和实现的快速诊断，不作为最终独立模型体系。

**候选主模型：低复杂度广义加性混合模型（GAMM）。**先仅用孕周、BMI 的加性平滑与孕妇随机截距，与已实现 LMM 公平比较；保持 logit 响应，不因简化结构而改成原浓度的高斯回归。现有管线已验证所有男胎浓度严格处于 `(0,1)`。不预设曲线方向或形状。

$$
\operatorname{logit}(y_{ij})=\beta_0+s_t(t_{ij})+s_B(B_{ij})+b_{0i}+\varepsilon_{ij}.
$$

- `t_ij`：检测孕周（周）；`B_ij`：BMI；`q_ij`：少量质控量；`b_0i,b_1i`：孕妇随机截距/斜率。
- 扩展顺序：先加性平滑随机截距，再单独检验交互 `ti(t,B)`；随机斜率、预先冻结的少量质控变量分别作为后续增量实验，不一次全部加入。结合效应稳定性、残差和样本外误差决定保留，不只看显著性。
- Python 可用 `patsy` 构造低维 B-spline，再用 `statsmodels.MixedLM` 实现样条混合模型近似；无平滑惩罚时须如实命名。需要自动平滑惩罚可使用 R `mgcv`/`gamm4` 并记录版本。
- 所有样条结点、中心化、填补和超参数选择均在训练折内完成；当前 LMM 完整交互设计对中心平移不变，但不能把这一特例推广到新模型。
- 样条自由度必须在按孕妇分组的交叉验证中选择，不凭图形随意增加。

#### 显著性与验证

- 仅在模型嵌套且满足检验条件时使用最大似然下的似然比检验；惩罚平滑和方差边界不能直接套普通卡方检验。用适用的近似检验或孕妇级 Bootstrap，并报告部分效应区间。
- 随机效应必要性、残差正态性/异方差、影响点和预测残差均需检查。
- 用孕妇级 Bootstrap 估计效应区间；对比 LMM 与 GAMM 的受试者级交叉验证误差、校准和残差结构。
- 文献只支持“孕周与体重可能影响胎儿游离 DNA 比例”的方向，不替代本附件中的统计检验。
- 分位数回归仅作为稳健性增强：在孕妇级 Bootstrap 下估计 Y 浓度的低分位趋势，检查均值模型是否掩盖低浓度人群；它不作为第三套最终模型。若低分位曲线交叉或重复测量推断不稳定，则只保留描述性结果。

#### 不建议

直接用所有 1082 行做 Pearson 相关与普通线性回归会把重复记录当独立样本，使显著性被夸大；单纯高阶多项式也容易产生边界振荡。

### 5.2 问题 2：BMI 分组与最佳时点

#### 推荐模型

**补充描述：Turnbull 区间删失曲线（非默认实验）。**只有额外接受吸收性与观测机制假设时，才以最后未达标至首次观测达标构造区间，并考虑左/右删失。当前存在 34 人回落，普通区间删失构造不能保证覆盖真实首次穿越时间；因此该曲线仅作为假设情景，不作为无误差生理时间估计或主决策依据。默认 baseline 见下述混合 Logistic。

**主路径：BMI-only 观测达标概率模型。**先混合 Logistic baseline，验证后才用低复杂度 GAMM-Binomial 扩展，二者属于同一二项混合模型族。附件没有预约或首次产检日期；固定 `B_i` 为该孕妇按日期/孕周排序后第一条记录的 BMI，仅作为预约时 BMI 的静态代理，未来记录不可回填。该代理不证明更早时点的 BMI 已知；历史首条检测孕周也不限制推荐时点。主网格为 11–25 周，按 BMI 与孕周联合报告观测支持人数，标记早于首条观测及稀疏区域的外推；另做首条观测不晚于 12 周的子样本敏感性分析。

令 `A_ij=1(Y_ij^obs≥0.04)`，只使用孕周与固定事前 BMI：

$$
\operatorname{logit}P(A_{ij}=1\mid t_{ij},B_i,u_i)=\eta(t_{ij},B_i)+u_i,\quad u_i\sim N(0,\sigma_u^2).
$$

baseline 取 `η=β0+βt t+βB B`；平滑扩展先取 `η=β0+s_t(t)+s_B(B)`，交互单独检验。用于新孕妇决策的概率须对随机效应积分：

$$
p_i(t)=\int\operatorname{logistic}\{\eta(t,B_i)+u\}\,\phi(u;0,\sigma_u^2)\,du.
$$

4% 是题面给出的主分析阈值，不通过交叉验证调优；3.5% 和 4.5% 只作为阈值/测量误差敏感性情景。可同时展示达到 90% 与 95% 可靠性水平的最早孕周，但这两个水平属于外部决策情景，不是由样本自动识别的“最优参数”。

积分可用 Gauss–Hermite 求积并检查数值精度；不能把随机截距置零后的条件概率当成人群边际概率。观测达标概率已包含数据中的检测波动，不再强制拆分潜在真实浓度与误差。Turnbull 仅作带吸收性假设的补充描述，不能把非吸收的阈值状态解释为已可靠识别的首次生理达标事件，也不承担主决策输入。

#### 分组与时点算法：有序动态规划

把 BMI 候选切点按从小到大排序。主分析的候选时点网格设为附件有数据支持的 `t∈{11,11+1/7,…,25}` 周；10–11 周缺乏本附件观测支持，不作主推荐，只能在明确外部依据后外推。对任意连续区间 `I` 和候选孕周 `t`，定义组内平均风险

$$
C(I,t)=\frac{1}{|I|}\sum_{i\in I}\left[R_{\mathrm{delay}}(t)+\lambda\{1-p_i(t)\}\right],
$$

其中风险分段无空隙地定义为：`11≤t<13` 为早期（完成孕周不超过 12 周）、`13≤t<28` 为中期、`t≥28` 为晚期；主网格虽止于 25 周，仍保留晚期定义用于情景扩展。各段分别赋外部情景代价 `r_E<r_M<r_L`。`λ` 是未达标导致复检/不可靠的规范性代价，不能由预测数据交叉验证识别；若没有权威临床代价，就对 `(r_E,r_M,r_L,λ)` 做预先声明的情景网格和 Pareto/稳定性分析，不宣称唯一权重。

初次时点决策不再另设由 GC、读段数、比对率或过滤率构成的 `QualityRisk(t)`，因为这些量只有检测完成后才可见。若后续研究第一次失败后的复检时点，应另建“复检决策”并只使用第一次检测已经获得的质控信息。

为最小化全体孕妇的平均风险，区间总代价必须按人数加权：

$$
\widetilde C(I)=|I|\min_t C(I,t).
$$

再用

$$
D(k,j)=\min_{h<j}\{D(k-1,h)+\widetilde C([h+1,j])\}
$$

精确寻找 `k` 个连续 BMI 组的最优切点。设置最小组人数，尝试 3–5 组；模型超参数与组数可依据受试者级样本外决策损失和复杂度惩罚选择，但规范性风险权重保持为外部情景参数。除以总人数只改变目标尺度，不改变最优切分。当前样本规模下，DP 可给全局最优离散切分，优先于遗传算法。

#### 检测误差分析

- 18 组重复测序仅支持误差尺度的探索性估计，不能承担潜变量识别。先检查重复次数与独立性：若两次误差独立同方差，则差值方差是单次误差方差的两倍；多重复记录不能把全部两两差当成独立样本。
- 在 logit 浓度尺度估计重复差的尺度，按上述假设换算单次误差尺度，并对 0.5、1、1.5 倍额外扰动做情景分析；逆变换保证浓度位于 `(0,1)`。按孕妇重采样、重新拟合和分组。该实验检验额外扰动稳健性，不声称还原真实浓度或完成误差校正。
- 以 4% 为主阈值，并固定采用 3.5% 与 4.5% 两个敏感性情景，报告切点、推荐周和风险的置信区间/稳定频率。
- 若不同损失权重下推荐时点剧烈变化，应给 Pareto/敏感性结果，而不是宣布唯一“最佳”。

### 5.3 问题 3：多因素达标概率与决策

#### 推荐模型

**主模型：多因素混合效应达标概率模型。**沿用问题 2 静态代理协议：BMI、年龄、身高、受孕方式、孕产次数均取首次记录当时已知值，缺失只在训练折填补，禁止借用未来记录。历史首条孕周不限制可行域，体重不与 BMI、身高同时直接进入。下面为可选交互的扩展上限，初始模型从线性项与随机截距开始：

$$
\operatorname{logit}P(Y_{ij}\ge0.04)=s_t(t_{ij})+s_B(B_i)+s_{tB}(t_{ij},B_i)
+\boldsymbol\theta^\top\mathbf x_i+u_i.
$$

**主路径冻结：**直接二项混合模型与 Q2 保持一致，先线性项、随机截距，后检验加性平滑和必要交互；预测新孕妇时按 Q2 对随机效应积分。连续浓度积分、AFT、潜变量误差模型和其他生存模型不进入默认实施清单，仅作为有额外依据时的补充研究。

Random Forest、XGBoost/GBDT 不进入默认实施清单；只有新的失败证据足以支持更换模型时才重新修订合同。

#### 验证与决策

- 外层按孕妇做 5 折交叉验证，内层只选择样条自由度、正则化强度等统计超参数；所有同一孕妇记录必须在同一折。决策风险权重采用外部设定的情景网格，不用标签拟合。
- 报告实际观测时点的二元 Brier 分数及按孕周分层的校准、达标比例误差；它们不是生存分析的时间依赖 Brier。未观测候选时点的反事实结果不能当真实标签。
- Q2/Q3 共用外层孕妇折和 DP。每个外层训练集内完成预处理、内层调参、组数选择、切点和时点优化，然后冻结策略用于外层留出孕妇；不得在全部 OOF 预测上挑完策略后用同一批数据报最终性能。
- 按孕妇等权汇总决策风险；留出数据未观测推荐时点时，只能报告明确标为模型依赖的预期风险与支持覆盖，不能宣称真实反事实收益或因果最优。额外变量消融使用相同外层折与外部风险权重。
- 对年龄、身高等变量做消融；只有显著降低样本外决策损失的变量才保留。

### 5.4 问题 4：女胎异常判定

#### 标签与任务重构

把 `AB` 拆成三个二元标签 `A13,A18,A21`，分别表示字符串中是否含 `T13/T18/T21`；总体异常为三者逻辑或。这样保留染色体类型并允许复合异常。不可把 `胎儿是否健康` 当标签，因为女胎该列全为“是”。

#### 推荐模型

**Baseline：单一染色体 Z-score 阈值规则。**对每条染色体用 `Z_c≥τ_c` 形成预设统计规则；`τ_c` 在训练折内选择，`τ=3` 仅作为三标准差参考点，而非未经来源核验的临床阈值。初步核对显示简单参考规则 `max(Z13,Z18,Z21)≥3` 仅命中 3/67 个 AB 阳性记录，说明本附件标签不能被该单阈值充分复现，baseline 必须保留但不能作为最终模型。

**主模型：Elastic-Net Logistic。**以总体标签 `A_any` 为主要性能展示，同时保留 `A13,A18,A21` 判定。先统计每个标签的阳性记录数、独立阳性孕妇数、标签重叠和各折类别覆盖，再决定各染色体模型复杂度；阳性少时降低特征维度、调整分组折数并报告不确定性，不能因此遗漏染色体类型。总体概率直接拟合，不能将三个相关概率相加或取逻辑或。候选特征为各染色体 Z 值、X 浓度、GC、读段数、比对率、重复率、过滤率、孕周、BMI、年龄；仅在训练折内选择低维集合。对标签 `c∈{13,18,21,any}` 的记录 `n`，定义

$$
p_{nc}=\sigma(\alpha_c+\mathbf x_n^\top\boldsymbol\beta_c),
$$

并最小化带类别权重的正则化对数损失

$$
-\sum_n w_{y_{nc}}\left[y_{nc}\log p_{nc}+(1-y_{nc})\log(1-p_{nc})\right]
+\lambda_1\|\boldsymbol\beta_c\|_1+\lambda_2\|\boldsymbol\beta_c\|_2^2.
$$

Elastic-Net 处理共线性并保留可解释系数；`λ1,λ2`、类别权重和判定阈值仅在内层分组验证中选择。染色体类型判定可对三个阈值化后的二元输出执行逻辑或，但总体概率、总体 Brier 分数和总体校准一律使用 `A_any` 的直接模型。四个二元模型共享特征处理、损失函数、正则化和验证流程，属于同一个模型体系。当前问题 4 只保留“Z 阈值 baseline + Elastic-Net Logistic”两个模型体系；SVM 仅作为文献背景，不列入候选实施方案。样本量与阳性数较小，不建议默认上 LightGBM/深度网络。

Random Forest、XGBoost/GBDT 也不进入问题 4 默认方案；若后续探索，必须使用相同的孕妇级外层折、相同输入特征和相同阈值选择规则公平比较，变量重要性不能解释为因果效应。

#### 防泄漏、阈值与评价

- 使用 `StratifiedGroupKFold` 或等价的按孕妇分层分组交叉验证；标准化、缺失填补、特征选择、SMOTE（若使用）均只能在训练折内部完成。
- 类别不平衡优先使用类别权重；如使用过采样，不得生成跨孕妇合成信息。
- 主要指标：每个染色体及总体异常的 PR-AUC、灵敏度、特异度、MCC、Brier/校准；准确率不能作为主指标。
- 分类阈值按漏诊与误报代价选择，并用孕妇级 Bootstrap 给出区间。Logistic 概率在外层验证折检查校准；只有训练折内部的 sigmoid 重新校准能稳定降低 Brier 分数时才采用。若目标是高敏感筛查，应先约束最低灵敏度，再在可行阈值中最大化特异度。
- AB 是附件给出的检测标签而非已确认临床金标准；最终结论只能表述为“对 AB 标签的判别能力”，不能越界声称临床诊断准确率。

## 6. 假设分级

### 关键假设（放宽会改变模型定义）

1. **孕妇代码唯一标识统计个体。**依据：附件存在同代码多次抽血/检测。若同代码可能代表不同妊娠，必须重新建立个体键并复验全部分组验证。
2. **问题 2–3 的决策只能使用预约时可知变量。**依据：时点选择发生在测序前；附件中以每名孕妇首条记录的静态协变量作为可复现代理，但首条历史检测孕周不限制推荐时点，并禁止使用未来记录回填。若允许检测后质控量，应另定义“复检决策”而非初次时点决策。
3. **AB 列是问题 4 要复现的目标标签，但不是临床金标准。**依据：题面明确指定 AB 为判定结果，而女胎出生健康列无变异。放宽后需外部确诊标签才能评价临床效能。

### 可放宽假设

1. logit 变换后残差近似高斯；若失败，改用 Beta 混合回归或稳健混合模型。
2. 个体随机效应服从正态分布；用稳健估计和孕妇级 Bootstrap 检查。
3. 同次抽血重复检测差可代表测量误差；用不同误差尺度和分布做敏感性分析。
4. 风险函数按 `[11,13)`、`[13,28)`、`[28,∞)` 无空隙分段；对外部情景权重进行网格扫描并报告策略稳定性。
5. BMI 分组必须连续且可解释；若连续个体化策略显著更优，仍保留分组策略作为题目要求的可执行答案并报告性能差距。

## 7. 实现依赖、候选图表与失败回退

### Python 依赖

`pandas`、`numpy`、`scipy`、`statsmodels`、`patsy`、`scikit-learn`；区间删失模型可选 `lifelines`/R `icenReg`，如 Python 实现无法稳定处理左右/区间删失，则明确使用 R 并记录版本，不得把中点插补伪装成精确事件时间。

### 候选图表

- Q1：孕妇轨迹图、孕周×BMI 二维部分效应图、随机效应/残差诊断图。
- Q2：附观测覆盖的边际达标概率曲面、BMI—时点风险热图、DP 分组切点及 Bootstrap 稳定性图；Turnbull 仅为可选假设情景。
- Q3：样本外校准图、变量消融图、多因素策略与 BMI-only 策略的风险差图。
- Q4：每染色体 PR 曲线、混淆矩阵、校准曲线、系数/SHAP（仅在主模型确立后）图。

### 失败条件与回退

- 随机斜率不收敛：退回随机截距，并报告模型比较。
- 样条边界不稳定：降低自由度或退回预先指定的二次项。
- 区间删失描述无法稳定估计：如实报告非唯一性与支持不足；主决策仍使用二项混合达标概率，不靠中点插补构造精确事件。
- BMI 某候选组样本过少：提高最小组人数、减少组数，不以遗传算法强行产生细分。
- Q4 某染色体阳性过少导致折内无阳性：减少折数或使用重复分组留出，禁止按行随机切分。

## 8. 文献与历史经验依据

1. Wang E. et al. (2013), *Gestational age and maternal weight effects on fetal cell-free DNA in maternal plasma*, Prenatal Diagnosis. DOI: [10.1002/pd.4119](https://doi.org/10.1002/pd.4119)。支持检验孕周与母体体重方向，但不替代附件分析。
2. Turnbull B. W. (1976), *The Empirical Distribution Function with Arbitrarily Grouped, Censored and Truncated Data*, JRSS B. [出版页面](https://academic.oup.com/jrsssb/article/38/3/290/7027379)。为区间/左右删失的非参数基线提供方法依据。
3. Yang J., Ding X., Zhu W. (2018), *Improving the calling of non-invasive prenatal testing on 13-/18-/21-trisomy by support vector machine discrimination*, PLOS ONE. DOI: [10.1371/journal.pone.0207840](https://doi.org/10.1371/journal.pone.0207840)。说明多 Z 值、临床和质控特征可用于机器学习判别，但文中的高准确率不能外推到本附件；本方案因样本规模与可解释性要求选择正则化 Logistic，而不是照搬 SVM。
4. 知识库 C023、C132 卡片均来自同题优秀论文，证据等级为 `single case`。可迁移启示是重复测量、删失和不平衡分类必须专门处理；其中 GPR、DeepHit、LSTM、GA 的选择仍需本数据的 baseline 对照证明。

论文检索由 OpenAlex 与 AnySearch 双引擎完成，以上引用在 DOI/出版页面进行了元数据核对。

## 9. 作者自检

### 下一轮最小实验（待执行）

1. Q1：复用已保存的孕妇级外层折，比较 LMM 与低维加性样条随机截距模型。所有新转换在训练折拟合；输出误差、残差和效应区间。现有 LMM OOF RMSE 为 0.5141、R² 为 −0.0302（logit 尺度），不预设非线性一定改善。
2. Q2：先混合 Logistic，检查边际达标概率的 Brier 与校准，再画附观测覆盖的孕周–BMI 曲面。BMI 方向和 90%/95% 可达性由数据检验；网格内未达到目标就报告未达到，不强行外推。概率验证通过后再进入 DP。
3. Q4：先输出各标签独立阳性孕妇数、重叠与分组折可行性表，再确定染色体级特征数和验证方式。

当前仅修订合同，不把上述待执行实验记为已完成。既有 `results/repro_manifest.json` 绑定上一版合同，保留为历史 P1 快照；合同哈希变化使其不再代表当前文档，下一次实验须生成新的完整快照并复验相关门禁。

- [x] 已覆盖全部四个子问题及附件字段。
- [x] 已区分检测行与独立孕妇，并规定受试者级验证。
- [x] 每题不超过两个独立模型体系。
- [x] 模型目的、公式、输入输出、参数、验证与失败条件已给出。
- [x] 时点决策排除了检测后才知道的质控变量。
- [x] 检测误差、删失、非单调回落和类别不平衡均已进入模型设计。
- [x] 文献已链接至 DOI 或出版页面；历史优秀论文未当作正确结论。
- [x] 未生成或虚构任何最终数值结果。
