# Large-file Policy

普通 Git 适合代码、文本、小型数据和最终图表，不适合大型视频、遥感栅格、模型权重或可再生成的批量中间结果。

大文件处理优先级：

1. 公开数据：记录稳定 URL、版本、许可证、SHA-256 和下载脚本。
2. 团队私有数据：存放在受控对象存储，仓库只记录访问说明与校验和。
3. 必须与 Git 版本绑定：使用 Git LFS，并在 PR 中说明体积和保留理由。
4. 可再生成结果：不提交，记录生成命令。

任何超过约 25 MB 的新增文件都应先审查；接近托管平台单文件限制时禁止直接提交普通 Git。
